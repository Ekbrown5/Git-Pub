<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>At foo bar</title>
  <style>
    body {
      background-color: #f0f0f0;
      color: #333;
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
    }

    h1 {
      text-align: center;
      padding: 20px;
      background-color: #007bff;
      color: #fff;
    }

    h2, h3 {
      text-align: center;
    }

    ul {
      list-style: none;
      padding: 0;
    }

    li {
      margin: 5px 0;
    }

    a {
      text-decoration: none;
      color: #007bff;
      cursor: pointer;
    }
  </style>
</head>

// when i go on the localhost, it gives me an error on line 47 and i cant figure out why
<body>
    <h1>At foo bar</h1>
    <h2><%= capitalizeFirstLetter(drinks[id].name) %></h2>
    <img src="<%= addPngExtension(drinks[id].image) %>" alt="<%= drinks[id].name %>">
    <h3>Price: $<%= drinks[id].price.toFixed(2) %></h3>
    <a href="/">Back</a>
  </body>

 
  <script>
   
    function capitalizeFirstLetter(str) {
      return str.charAt(0).toUpperCase() + str.slice(1);
    }


    function addPngExtension(url) {
      return url + '.png';
    }
  </script>
</body>

</html>