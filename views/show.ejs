<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>At foo bar</title>
  <style>
    body {
      background-color: #f0f0f0;
      color: #333;
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
    }

    h1 {
      text-align: center;
      padding: 20px;
      background-color: #007bff;
      color: #fff;
    }

    h2, h3 {
      text-align: center;
    }

    ul {
      list-style: none;
      padding: 0;
    }

    li {
      margin: 5px 0;
    }

    a {
      text-decoration: none;
      color: #007bff;
      cursor: pointer;
    }
  </style>
</head>
<h1>At Foo Bar</h1>
<h2><%= capitalizeFirstLetter(drink.name) %></h2>
<img src="<%= drink.image %>" alt="<%= drink.name %> Image">
<h3>Price: $<%= drink.price %></h3>
<a href="/drinks" style="display: block; margin-top: 10px;">Back</a>
` }) %>

<script>
// function to capitalize the first letter of a string
function capitalizeFirstLetter(str) {
  return str.replace(/^./, (char) => char.toUpperCase());
}
</script>
</body>

</html>